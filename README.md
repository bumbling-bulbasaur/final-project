# final-project

The data directory contains all data files used in project. There is also a tidy version of all the data called `tidy_electric_data.csv`. The web-scraping folder contains a short script used to download the NOAA weather data using its API.
